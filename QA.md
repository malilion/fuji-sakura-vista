# 場景初版驗證

## 已完成

- 使用 Blender 4.5.3 LTS 的 `bpy` 模組，全程以背景程序建立及渲染，未開啟 Blender 桌面視窗。
- `.blend` 已重新載入檢查；3,248 個物件、五個獨立主屋頂網格、90 片帶動畫的空中花瓣。
- 場景採公尺單位，攝影機動畫為 24 fps、1–288 格。
- 第 1、144、288 格的富士山峰與塔頂投影均在攝影機框內，詳見 `validation.json`。
- 已目視檢查開頭與中段動態預覽，以及第 288 格的 1920×1080 靜幀。
- 材質皆為程序節點，櫻花與建築由腳本建立；無外部貼圖或模擬快取依賴。
- 靜幀已用 Cycles / Metal、96 samples 渲染完成，影格輸出與時間紀錄位於 `renders/still/`。
- 動態預覽的 96 張影格已全部渲染；MP4 經 ffprobe 驗證為 640×360、8 fps、96 格、12.000 秒，紀錄見 `renders/preview_video_manifest.json`。
- 已目視檢查動態預覽第 1、142、286 格的構圖與物件完整性。
- 原始碼可通過 Python 語法編譯檢查。

## 本版定位

這是可繼續製作的程序場景初版，提供完整的建場、重建、鏡頭動畫與渲染流程。它不是攝影測量成果，也尚未達到參考照片的寫實精修程度。

後續精修重點：

1. 用實景比例與屋面資料細化忠靈塔的屋簷、相輪和表面材質。
2. 增加櫻花的品種特徵、自然樹幹分叉與前景枝梢細節。
3. 精修富士山地形、雪線與侵蝕溝紋，削弱程序生成的規律性。
4. 增加石階與牆面的非均勻老化，讓植被與地面的接觸更自然。
5. 以較高取樣渲染正式 24 fps 序列，檢查細小花瓣與樹梢的時間穩定性。

動態預覽採 640×360、8 fps、8 samples，以檢視動作為主；正式序列的渲染命令已附於 `README.md`。

## 網頁版（Three.js）檢查

- `scripts/export_web.py` 自 `.blend` 匯出 6.4 MB Draco GLB：20 份網格、33 個材質、富士山帶 `COLOR_0` 積雪頂點色；`web/assets/export_manifest.json` 記錄抽樣比例與每幀三角形數（約 11.5M）。
- 瀏覽器實測：88 draw calls，主構圖與第 288 格 Cycles 靜幀一致（塔、富士山峰、燈籠、欄杆位置對齊）；12 秒推進重播、軌道攝影機、微風、飄落花瓣、陰影開關、全螢幕、中 / 英切換皆可運作。
- 版面：桌面與 375 px 手機寬度皆無水平溢出；無 WebGL 時退回靜幀。
- 已知取捨：程序材質的噪聲細節與凹凸未帶入網頁版（僅平均色與粗糙度）；花瓣次表面與半透明以自發光微量近似；遠景櫻花僅保留 11% 花朵。

## v1.1 精修（2026-09-08）

依初版列出的方向完成四項，並以第 1、144、288 格 960 px 預覽與第 288 格 1920×1080 / 96 spp 實際渲染確認：

1. **忠靈塔**：每層寬度收分由 0.40 m 增至 0.50 m、屋簷半寬收分 0.42 m；新增四角戧脊（銅捲脊 + 青銅脊端）、每根立柱上方三層斗栱、每面兩扇格子窗；銅屋面加入大尺度銅綠斑塊。
2. **櫻花**：`SPECIES` 表定義六種樹型（染井吉野主樹、山櫻、老樹橫展、染井吉野、垂枝、幼樹），各自有主幹傾斜、分枝數、展幅、下垂與花色權重；主幹改為三段傾斜；花朵改為短枝上 3–6 朵的繖形花序，內層枝條也有較稀的花序，避免直立樹型在畫面內露出光禿枝幹。
3. **富士山**：`relief()` 以 23 / 41 / 77 / 131 四個頻段、噪聲擾動相位的 V 形溝取代單一正弦；溝深隨坡度向下加深；雪線沿溝底成舌狀延伸，西南迎風面覆蓋減少 4.5%，邊緣噪聲加大。
4. **石階與砌牆**：每塊踏面獨立沉降 / 傾斜 / 崩角（倒角 0.012–0.048）；中央踏面 45% 為磨光石材、7% 為地衣石；踏面縫隙隨機青苔。砌牆石塊尺寸 0.42–1.0 m、突出 ±0.08 m、22% 地衣石；青苔簷板改為 62% 出現且尺寸不一。

結果：3,759 物件、36.6M 實例多邊形；`validate_scene.py` 通過（戧脊脊端改名為 `Hip ridge cap` 以免被計入屋面數）。網頁版重新匯出為 7.9 MB、約 13.0M 三角形 / 幀。

未完成：正式 24 fps 序列（單格約 272 s，288 格約 22 小時），可用 `render_scene.py --preset animation` 分段執行。
